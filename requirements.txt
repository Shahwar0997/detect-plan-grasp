# Local env — torch-free (training happens on Colab; see requirements-colab.txt).
# Data prep, ONNX/INT8 inference, benchmarking, 3D lift, and the MuJoCo grasp sim.
onnxruntime>=1.17
onnx>=1.16
sympy>=1.12           # quantization shape-inference dependency
numpy>=1.24
opencv-python>=4.9
mujoco>=3.2           # grasp sim (Apple-Silicon wheel; replaces PyBullet, which won't build on macOS arm64)
huggingface_hub>=0.23 # pull YCB base+models from bop-benchmark/ycbv
matplotlib>=3.7
tqdm>=4.66
pyyaml>=6.0
